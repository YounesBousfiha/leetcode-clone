# ============================================================
# build.yml - Build all services and Docker images
# ============================================================
name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  REGISTRY: ghcr.io

jobs:
  build:
    name: Build & Package
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build with Maven
        run: mvn clean package -DskipTests --batch-mode

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: service-jars
          path: '**/target/*.jar'
          retention-days: 5

  docker:
    name: Build Docker Images
    needs: build
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    permissions:
      contents: read
      packages: write

    strategy:
      matrix:
        service:
          - { name: auth-service, module_path: services/auth-service, service_root: services }
          - { name: user-service, module_path: services/user-service, service_root: services }
          - { name: problem-service, module_path: services/problem-service, service_root: services }
          - { name: judge-service, module_path: services/judge-service, service_root: services }
          - { name: ai-assist-service, module_path: services/ai-assist-service, service_root: services }
          - { name: notification-service, module_path: services/notification-service, service_root: services }
          - { name: api-gateway, module_path: infrastructure/api-gateway, service_root: infrastructure }
          - { name: eureka-server, module_path: infrastructure/eureka-server, service_root: infrastructure }
          - { name: config-server, module_path: infrastructure/config-server, service_root: infrastructure }

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build service
        run: mvn clean package -DskipTests --batch-mode -pl ${{ matrix.service.module_path }} -am

      - name: Validate matrix mapping
        run: test -d "${{ matrix.service.service_root }}/${{ matrix.service.name }}"

      - name: Normalize image prefix
        run: echo "IMAGE_PREFIX=${GITHUB_REPOSITORY_OWNER,,}/leetcode" >> "$GITHUB_ENV"

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: infrastructure/Dockerfile
          build-args: |
            SERVICE_NAME=${{ matrix.service.name }}
            SERVICE_PATH=${{ matrix.service.service_root }}
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.service.name }}:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-${{ matrix.service.name }}:${{ github.sha }}

